apiVersion: v1
kind: Service
metadata:
  name: solo-leveling-notification
spec:
  selector:
    app: solo-leveling-notification
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
      name: http